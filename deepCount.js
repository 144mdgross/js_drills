// get the deep count of a multiple dimensional array, as well as a flat array
// example input: deepCount(["x", "y", ["z"]]);  => 4

function deepCount(a){
  // code here
  // strategy:
  // count primitive dataTypes
  // if somethings type is == Object, then count it and go deeper
}
